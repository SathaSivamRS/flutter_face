import 'package:flutter/material.dart';

class FaceDataPage extends StatelessWidget {
  const FaceDataPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Face Data")),
      body: const Center(child: Text("Face Data Page")),
    );
  }
}
